# Developer environment overrides.
# End users configure via the Settings panel in the UI (stored in ~/.freecad-agent/config.json).

# Override the FreeCAD RPC server address if not using the default.
FREECAD_HOST=127.0.0.1
FREECAD_PORT=65432
